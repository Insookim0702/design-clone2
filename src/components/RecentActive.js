import styled from "styled-components";

const Title = styled.header`
    font-weight: 800;
`;
function RecentActivity() {
    return <Title>Recent Activity</Title>;
}

export default RecentActivity;
